# Raw

`raw/` stores source snapshots and collection state.

Use `raw/inbox/` for new source material. Raw files should preserve source identity, link, collected time, and minimum context.

Do not write long-term conclusions here.

