# Config

`config/` stores local source registry configuration.

Keep credentials out of this directory. Use ignored local env files or environment variables.

Use `source-registry.yaml` to list sources that can write raw snapshots into `raw/inbox/`.

